{
  "name": "soft-melanin-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Soft Melanin MCP - Softness Strategist™ Content Engine",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:generate": "pnpm --filter @soft-melanin/web db:generate",
    "db:push": "pnpm --filter @soft-melanin/web db:push",
    "db:studio": "pnpm --filter @soft-melanin/web db:studio"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.3.0"
  },
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=18"
  }
}
